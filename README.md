["config/config.yaml`](config/config.yaml:1) to customize:\n\n```yaml\n# Camera settings\ncamera_id: 0                    # Camera device ID (0 = default camera)\nframe_width: 1280              # Frame width in pixels\nframe_height: 720              # Frame height in pixels\n\n# Circle detection parameters\nmin_radius: 28                 # Minimum circle radius in pixels\nmax_radius: 110                # Maximum circle radius in pixels\n\n# Edge detection & morphology\ncanny_low: 40                  # Lower threshold for Canny edge detector\ncanny_high: 120                # Upper threshold for Canny edge detector\ndilate_iter: 2                 # Dilation iterations (helps connect faint edges)\nerode_iter: 0                  # Erosion iterations\nclose_iter: 1                  # Morphology close iterations (bridge micro-gaps)\n\n# Classification thresholds\nper_ratio_min_good: 0.72       # Minimum for GOOD circles (smooth/mostly-complete)\nper_ratio_bad: 0.50            # Below this = BAD (dashed/broken)\naxis_ratio_max: 1.15           # Above this = BAD (ellipse reject)\nstd_norm_good: 0.08            # Below this = GOOD (smooth edge)\nstd_norm_bad: 0.14             # Above this = BAD (jagged)\n\n# Paper gating settings\npaper_only: true               # Ignore dark/non-paper interiors (hands/phones)\npaper_v_min: 120               # HSV V brightness inside circle (lower = more tolerant)\n\n# Output settings\noutput_dir: \"output", "Directory for saved images\n```\n\n## \ud83d\udda8\ufe0f Example Circle Patterns\n\n### Good Circles:\n\n- Perfectly drawn circle\n- Smooth edges\n- Consistent line thickness\n- Well-centered in frame\n- Complete perimeter (no gaps)\n\n### Bad Circles:\n\n- Ellipse instead of circle\n- Broken/dashed circle\n- Very thick/thin lines\n- Partially out of frame\n- Irregular shape\n- Jagged edges\n\n## \ud83d\udcca Testing Your System\n\n```bash\n# Test with different camera\npython circle_inspector_tuned.py --camera 1\n\n# Test with configuration file\npython circle_inspector_tuned.py --config config/config.yaml\n```\n\n## \ud83c\udfae Controls\n\n| Key | Action |\n|-----|--------|\n| `q` | Quit program |\n| `s` | Save current image |\n| `c` | Print circle details to console |\n\n## \ud83d\ude80 Performance Tips\n\n1. **Good Lighting**: Ensure even lighting on the circle\n2. **Contrast**: Use dark ink on light paper\n3. **Steady Hand**: Hold paper still for better detection\n4. **Background**: Use plain background (not busy patterns)\n5. **Distance**: Hold circle 30-50cm from camera\n\n## \ud83d\udc1b Troubleshooting\n\n**No circles detected?**\n\n- Increase lighting\n- Use thicker pen\n- Adjust `min_radius` to smaller value\n- Check camera connection\n- Lower `canny_low` and `canny_high` thresholds\n\n**False detections?**\n\n- Increase `per_ratio_min_good` threshold\n- Decrease `axis_ratio_max` threshold\n- Enable `paper_only` to filter out non-paper objects\n- Clean camera lens\n- Use plain background\n\n**Low FPS?**\n\n- Reduce resolution: Change `frame_width/height` in config\n- Skip frames in processing loop\n- Close other applications\n\n**Too many BAD classifications?**\n\n- Lower `per_ratio_min_good` (e.g., 0.65)\n- Increase `per_ratio_bad` (e.g., 0.40)\n- Increase `std_norm_good` (e.g., 0.12)\n- Increase `std_norm_bad` (e.g., 0.18)\n\n## \ud83d\udcdd License\n\nThis project is for educational purposes.\n\n## \ud83e\udd1d Contributing\n\nFeel free to submit issues and enhancement requests!\n\n---\n\n**Happy Circle Detecting! \ud83c\udfaf**\n\n---\n\n## \ud83d\udcf1 Related Project: 2D Barcode Detection\n\nLooking for 2D barcode detection? Check out our separate project:\n\n**2D_Barcode_Dectector** - Detects and validates QR codes, Data Matrix, and other 2D barcodes.\n\nLocation: `../2D_Barcode_Dectector/`\n\n---\n\n## \ud83e\udd16 AI Prompt for Vibe Coding This Program\n\nUse the following prompt to generate this program using AI coding assistants:\n\n```\nCreate a Python circle detection and quality inspection program with the following specifications:\n\nLIBRARIES & TOOLS:\n- opencv-python (cv2) - for image processing, edge detection, contour analysis\n- numpy - for numerical operations and array handling\n- pyyaml - for configuration file loading\n\nCORE FUNCTIONALITY:\n- Real-time webcam circle detection using OpenCV\n- Contour-first approach (NOT Hough Circle Transform) for robust detection\n- Classify circles as \"GOOD\" or \"BAD", "based on multiple quality metrics\n- Display results with color-coded overlays (green=GOOD, red=BAD)\n- Save annotated images on demand\n\nDETECTION PIPELINE:\n1. Capture frame from webcam (1280x720, 30fps)\n2. Convert to grayscale and apply CLAHE (clipLimit=2.0, tileGridSize=8x8)\n3. Apply Gaussian blur (5x5 kernel)\n4. Canny edge detection (low=40, high=120)\n5. Morphology operations: dilate(2 iterations), close(1 iteration)\n6. Find contours using cv2.findContours\n7. Filter contours by radius (28-110 pixels)\n8. Extract features from each contour\n\nFEATURE EXTRACTION:\n- Center (cx, cy) and radius (r) using cv2.minEnclosingCircle\n- Perimeter ratio: arcLength / (2\u03c0r) - measures completeness\n- Axis ratio: major/minor ellipse axes - detects distortion\n- Edge smoothness: std(distance_to_center)/r - measures irregularity\n- Mean HSV V value inside 0.6r disk - for paper gating\n\nCLASSIFICATION RULES:\n- GOOD: per_ratio >= 0.72 AND std_norm <= 0.08\n- BAD (incomplete): per_ratio < 0.50\n- BAD (ellipse): axis_ratio > 1.15\n- BAD (jagged): std_norm > 0.14\n- BAD (uncertain): doesn't meet GOOD criteria but not clearly BAD\n- Paper gating: ignore if mean_v < 120 when paper_only=True\n\nDEDUPLICATION:\n- Sort by per_ratio (descending) and std_norm (ascending)\n- Remove circles with centers within 0.5 * min(radius) distance\n\nUSER INTERFACE:\n- Display annotated frame with circles\n- Show FPS, total circles, GOOD count, BAD count\n- Method indicator: \"OpenCV Traditional (Contour-first)", "Controls: q=Quit, s=Save, c=Details\n\nCONFIGURATION:\n- Load parameters from YAML config file (optional)\n- Default values for all parameters\n- Support command-line args: --camera, --config\n\nOUTPUT:\n- Save annotated images to output/ directory\n- Print detailed metrics when 'c' is pressed\n\nPARAMETERS (default values):\n- Camera: frame_width=1280, frame_height=720, camera_id=0\n- Radius: min_radius=28, max_radius=110\n- Edge: canny_low=40, canny_high=120\n- Morphology: dilate_iter=2, erode_iter=0, close_iter=1\n- Classification: per_ratio_min_good=0.72, per_ratio_bad=0.50, axis_ratio_max=1.15, std_norm_good=0.08, std_norm_bad=0.14\n- Paper gating: paper_only=True, paper_v_min=120\n- Output: output_dir=\"output", "SKILLS REQUIRED:\n- OpenCV image processing (grayscale conversion, CLAHE, Gaussian blur, Canny edge detection)\n- Contour analysis (findContours, minEnclosingCircle, arcLength, fitEllipse)\n- Morphological operations (dilate, erode, morphologyEx)\n- HSV color space analysis\n- NumPy array operations\n- YAML configuration handling\n- Real-time video processing with cv2.VideoCapture\n- GUI display with cv2.imshow and keyboard handling\n\nIMPLEMENTATION NOTES:\n- Use cv2.CAP_PROP_FRAME_WIDTH/HEIGHT to set camera resolution\n- Use cv2.createCLAHE for adaptive histogram equalization\n- Use cv2.Canny for edge detection\n- Use cv2.findContours with RETR_LIST and CHAIN_APPROX_NONE\n- Use cv2.minEnclosingCircle for center and radius\n- Use cv2.arcLength for perimeter calculation\n- Use cv2.fitEllipse for axis ratio (handle exceptions)\n- Use cv2.morphologyEx with MORPH_CLOSE for gap bridging\n- Implement FPS counter with time-based averaging\n- Handle camera read failures gracefully\n- Create output directory if it doesn't exist\n```"]